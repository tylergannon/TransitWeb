import type { BodyGraphProps } from './types';

const props: BodyGraphProps = {
	channelSpace: 2,
	pipRadius: 19,
	distFromEdge: 5,
	aspectRatio: 0.83,
	width: 600,
	scale: 1,
	triangleSize: 150,
	squareSize: 162
};

export default props;
